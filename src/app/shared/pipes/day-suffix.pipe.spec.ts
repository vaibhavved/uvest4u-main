import { DaySuffixPipe } from './day-suffix.pipe';

describe('DaySuffixPipe', () => {
  it('create an instance', () => {
    const pipe = new DaySuffixPipe();
    expect(pipe).toBeTruthy();
  });
});
